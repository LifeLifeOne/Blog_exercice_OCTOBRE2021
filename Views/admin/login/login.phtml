<div class="container mt-5">
    <h1 class="text-center mb-5">Espace Administration</h1>
    <hr>

    <div class="card rounded shadow mx-auto mt-5 p-3" style="width: 300px">

        <!-- FORMULAIRE -->
        <form class="row mt-3 d-flex justify-content-center" method="POST">

            <!-- Email  -->
            <div class="mb-3 row">
                <label for="email" class="">Login:</label>
                <div>
                    <input type="text" name="email" class="form-control" id="email" placeholder="email@exemple.com">
                </div>
            </div>

            <!-- Password  -->
            <div class="mb-3 row">
                <label for="password" class="">Mot de passe:</label>
                <div>
                    <input type="password" name="password" class="form-control" id="password">

                    <!-- ERREUR BOTH -->
                    <div class="container-msg">
                        <?php if(!empty($message['error']['admin'])): ?>
                            <span class="text-danger">
                            <?php foreach($message as $key => $msg): ?>
                                * <?= $msg['admin'] ?>
                            <?php endforeach ?>
                            </span>
                        <?php endif ?>
                    </div>

                </div>
            </div>

            <!-- Submit  -->
            <div class="col-auto">
                <button type="submit" name="login" class="btn btn-primary mb-3">Connexion</button>
            </div>
        </form>
    </div>

</div>